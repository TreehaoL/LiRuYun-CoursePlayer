# 这个仓库只放"给别人用"的文件：脚本 + 文档 + 许可证。
# 开发用的源码与工具（生成器、测试、模块）**故意不在这里** —— 留本地。

# 纯逻辑版（不带面板的那个，LiRuYun-v3.user.js）**不发布**：对外只留一个安装文件。
# 它只在本地构建，万一被 make-release.mjs 旧版本写进这个目录，也不要提交上去。
可选-纯逻辑版/

# 编辑器 / 系统杂项
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# 临时文件
*.tmp
*.bak
*~
*.log
