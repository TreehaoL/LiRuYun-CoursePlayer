# 这个仓库只放给大家用的东西：脚本 + 文档 + 许可证。开发用的生成器和测试在本地，不在这里。

# 不带面板的那个构建（LiRuYun-v3.user.js）不发布，只在本地构建；
# 万一被旧版打包脚本写回这个目录，也不要提交。
可选-纯逻辑版/

# 编辑器与系统杂项
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# 临时文件
*.tmp
*.bak
*.log
