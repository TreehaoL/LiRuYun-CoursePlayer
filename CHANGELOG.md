# 变更记录

> 只看"每一版改了什么"就看这份。想了解为什么这么改（现场反馈、页面结构、踩过的坑），
> 见仓库的开发文档。

版本号规则：正式版（带面板）走自己的发布线，当前 **1.0.0**；纯逻辑版 `LiRuYun-v3.user.js` 走逻辑线，当前 **3.4.0**。
两个文件的**功能逻辑完全一样**，差别只在面板布局。
（`5.0.0`–`5.5.0` 是面板功能的开发期编号，功能定型后正式化为 `1.0.0` —— 见下面第一条。）

---

## 1.0.0（当前 · 首个正式版）

- **定名与定版**：脚本正式取名 **砺儒云课程播放助手**，版本号从开发期的 `5.5.0` 定为 `1.0.0`。
  **功能与 5.5.0 完全一致，没有任何删减**，只是名字和版本号正式了。
- **脚本文件名也正式了**：`LiRuYun-v3pro.user.js` → **`LiRuYun-CoursePlayer.user.js`**
  （发出去的文件名里不再带开发期代号；`CoursePlayer` = 课程播放助手）。
  ⚠ 如果你之前是从仓库 Raw 链接安装的，旧链接会失效 —— 请用新文件名重新安装一次。
- 顺带把源码里过期的版本注释（4.x 的四页签说明、"pro-5.1" 这类内部编号）清理干净，
  控制台横幅现在会打印 `脚本已注入 v1.0.0`。
- **共存判定补了一条规则**：正式版与纯逻辑版的版本号不在同一条线上（1.0.0 对 3.4.0），
  光比版本大小会误判成"对方更新"；现在按**带面板的优先**让位 ——
  同一页只留一个面板（`test-version-coexist` 的 C 场景守着这条）。
- ⚠ 升级提醒：开发期版本（`砺儒云播放助手 v3pro · 分页签面板`）与本版 `@name` 不同，
  Tampermonkey 视为两个脚本。装好 1.0.0 后请到 Tampermonkey 里把 5.x 那条**关掉开关**。
- **支持自动更新**：脚本头部写入 `@downloadURL` / `@updateURL`，都指向
  `https://raw.githubusercontent.com/TreehaoL/LiRuYun-CoursePlayer/main/LiRuYun-CoursePlayer.user.js`。
  从 Raw 链接装过一次以后，仓库里的 `@version` 一变大，Tampermonkey 就会自动提示更新，
  不需要再手动复制粘贴重装。
  （如果你是在这一条加上之前就装好的，请用上面的链接**重新安装一次**，把这两个地址带进脚本头；
  之后就能自动更新了。）

## v3pro 5.5.0

- **视频列表与模式页之间新增可拖分隔条**（现场："播放列表和下面的参数设置之间不能用户自由调整大小"）：
  上一版只加了**日志**那一根；现在两根都在 —— 列表下面那根**往下拖列表变高**，
  日志上面那根**往上拖日志变高**，松手即记住（`listH` / `logH`）。
  加上面板右下角原本就有的缩放手柄，**三处尺寸都能自己调**。


## v3pro 5.4.0

- **偏好设置常驻设置窗口**：以前是"打开设置把设置块搬进弹窗、关闭再搬回面板"，用户看到的是
  "打开→东西出现，关闭→东西消失"。现在设置项**一直住在设置窗口里**，打开/关闭只是显示/隐藏，
  主面板结构一点不动。（现场提问："在逻辑上是否有误？"→ 确实有误，改了。）
- **日志能看全、有滚动条**：根因有三处 —— 日志缓冲区只留 10 行、UI 层又裁到 5 行、
  外层容器没有被限高（内容把整块撑破再被裁掉，所以滚动条永远不出现）。现在缓冲区 400 行、
  整段写入、两层限高 + `overflow-y:scroll`。
- **日志可滚动查看历史**：默认跟随最新；你往上翻（离开底部 8px 以上）之后就不再抢你的滚动条，
  标题行出现 `· ⤓ 最新`，点一下跳回最新。
- **日志区高度可拖**：日志上面加一条分隔条，往上拖变大，松手记住。

## v3pro 5.3.0

- **日志不再刷屏**：同一条内容 60 秒内只写前两条，之后折叠成汇总；
  关不掉的站点提示条最多试 3 次就彻底安静。
- **暴力模式达标后自动催页面读数**：暂停 → 350ms → 播放 → 再等 700ms 重读，
  日志写 `41.2% → 100%`；撞硬上限前也先催一次（避免白刷一整轮）。
- **风险确认只记"当次会话"**：确认一次后，同一标签页内刷新 / 批量换页不再问；
  关掉标签页即失效。暴力页可一键改回"每次都问"。
- 修掉"连播开关刷新后显示成关"（建行时没同步标签与存储）。

## v3pro 5.2.0

- 恢复「—」最小化（收成一条标题栏）与 ◎ 悬浮球。
- 新增三个开关：**完成后是否自动下一节**、**自定义达标进度**、**暴力页点播放时按列表连播**。
- 新增 **⏹ 急停**：停掉队列 / 暴力 / 批量并暂停视频，之后不会被自动恢复。

## v3pro 5.1.0 / v3 3.1.0

- **站点原生弹窗自动「确定」**：把页面自己的 `window.alert` 换掉，弹窗根本不会出现，
  等价于"已经点了确定"且不卡页面；顺手恢复被站点暂停的播放、关掉过时的黄条。
- `confirm` 只对白名单（"同时观看 / 多个视频 / 当前视频已暂停"）自动答应，其它照常问你。
- 同源 iframe 一起接管；万一没拦住会记一条 `页面被原生弹窗卡住 N.Ns`。

## v3pro 5.0.0 · v3 3.4.0

- **面板重排**：顶上「安全模式 / 暴力模式」两个按钮分页；视频列表常驻；
  「⚙ 设置」弹出独立窗口；日志常驻主窗口底部。
- 日志写满（最多 80 行、框内滚动）；列表刷新后当前视频自动滚到中间；自动静音。

## v3 3.x（逻辑版）

- **自适应视频列表**：扫描所有候选容器并择优、逐个展开折叠章节（Bootstrap collapse）、
  用 Moodle 官方只读接口 `core_course_get_contents` 服务端兜底，再按课程顺序合并。
  修掉了"第一门课收满 3 条就收手"导致的列表明显不全。
- **暴力模式也带视频列表**，两种模式下都可见，点一行跳一节。
- 参数 / 诊断分区可折叠并记住展开状态。
- 收敛的看护：停摆看护、冻帧看护、学习确认自动通过、自动重播、定时暂停、暂停保活。

---

## 更早的版本

- `v2.0.12`（`LiRuYun-SafePlusBrute.user.js`）：早期发布版，已冻结不再改动。
  它**没有让位机制**，和别的版本同页会各建一个面板，务必手动关掉一个。