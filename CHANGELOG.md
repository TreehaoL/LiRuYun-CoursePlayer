# 变更记录

> 这一版就是**第一个公开版本**。下面按"这块功能做了什么"列，不按开发过程列。
> 以后每次发新版，在这一节上面加一节即可。

**1.0.0**（当前）—— 首个公开版本，功能与说明如下。

---

## 视频清单

- 自动把**整门课的视频**列出来，每节带进度条、百分比、`已完成` 标记，点一行直接跳过去。
- 扫描所有候选容器并择优，逐个展开折叠章节（Bootstrap collapse），再用 Moodle 官方只读接口
  `core_course_get_contents` 从服务端兜底，最后按课程顺序合并 —— 换一门课也不会少节。
- `只看未完成`、`刷新列表`、`扫描`；列表刷新后自动把当前视频滚到可见位置。
- 两种模式（安全 / 暴力）下列表都常驻可见。

## 连播与进度

- **队列连播**：一节达标自动切下一节（可关）。
- 直接用课程页的完成要求（默认 90%，可自定义）+ 真实进度判定，**不拿页面上的"已完成"字样当判据**。
- 暴力模式达标后**自动催一次页面读数**（暂停 → 350ms → 播放 → 再等 700ms 重读），
  日志会写 `41.2% → 100%`，不用手动刷新。

## 播放看护

- **停摆看护**：播放被暂停时试着恢复；**冻帧看护**：画面不动时重新 play 并轻微 seek 唤醒。
- **学习确认自动通过**（多事件类型自适应），完成后恢复播放。
- **站点原生弹窗自动「确定」**：把页面自己的 `window.alert` 换掉，弹窗根本不会出现
  （"禁止同时观看多个视频"那类），并顺手恢复被站点暂停的播放、关掉过时的黄条。
  `confirm` 只对白名单自动答应，其它照常问你。
- 自动静音、定时暂停（每隔 N 分钟停 M 秒）、暂停保活、自动重播。

## 面板

- 顶上「安全模式 / 暴力模式」两个按钮**分页**，各自一页；视频列表常驻；
  「⚙ 设置」弹出独立窗口（设置项常驻在里面，开关只是显示/隐藏）；日志常驻主窗口底部。
- **日志能看全**：缓冲区 400 行、两层限高 + 滚动条；默认跟随最新，
  往上翻（离开底部 8px 以上）就不再抢滚动条，标题行出现 `· ⤓ 最新`，点一下跳回最新。
- **日志不刷屏**：同一条内容 60 秒内只写前两条，之后折叠成汇总。
- **三处尺寸都能自己调**：列表高度、日志高度（两根分隔条，拖动即记住）、面板大小（右下角手柄）。
- 「—」最小化成一条标题栏、`◎` 收成悬浮球；面板透明度可调。
- **⏹ 急停**：一键停掉队列 / 暴力 / 批量并暂停视频，之后不会被自动恢复（点播放即可恢复）。

## 暴力模式

- 直接向 `mod_fsresource_set_time` 连续上报观看时长，把进度"刷"到达标。
- **三道闸门**：默认关闭；启动前有风险确认框（确认一次只在**当次会话**内免问，关掉标签页即失效，也可以改回"每次都问"）；
  达标即停 / 次数上限 / 连续失败即停。
- 撞硬上限前也会先催一次页面读数，避免白刷一整轮。
- ⚠ 风险请自己评估：会让学校服务器收到异常请求，可能与课程 / 学校的学术诚信要求冲突，作者不对使用后果负责。

## 自动更新

- 脚本头部写入 `@downloadURL` / `@updateURL`，都指向
  `https://raw.githubusercontent.com/TreehaoL/LiRuYun-CoursePlayer/main/LiRuYun-CoursePlayer.user.js`。
- 从上面这个链接装过一次以后，仓库里的 `@version` 一变大，Tampermonkey 就会自动提示更新，
  不用再手动复制粘贴重装。
