# 使用说明 · 砺儒云课程播放助手 1.0.0

这份文档是**给人看的操作手册**：装上以后每一步做什么、每个开关什么效果、出问题先看哪里。
（想了解"为什么这么设计"、踩过哪些坑，见仓库里的开发文档。）

> **安装一次就够，之后会自动更新。** 脚本头部写着 `@downloadURL` / `@updateURL`，指向仓库
> `TreehaoL/LiRuYun-CoursePlayer` 的 `main` 分支；仓库里的 `@version` 一变大，Tampermonkey
> 会自动提示更新，不用再复制粘贴。用方式 A（手工粘贴）装的也能自动更新 —— 那两个地址在脚本正文里。

---

## 0. 三十秒上手

1. 装好 `LiRuYun-CoursePlayer.user.js`（Tampermonkey → 新建脚本 → 粘贴 → 保存）；
2. 打开任意课程的**视频页**（`/mod/fsresource/view.php?id=…`）或**课程页**；
3. 页面右边会出现一个面板：
   - 想手动播：点面板上的 `▶ 播放 / 重播`；
   - 想让它自己一节节放完：点 `自动连播本页`（会按当前视频所在列表往下播）；
   - 想看清整门课：打开课程页 → 面板里就是完整列表，点任意一行直接跳过去。
4. 想安静一会儿：`⏹ 急停`（停止一切并暂停视频）；想继续：点 `▶ 播放`。

---

## 1. 面板长什么样、每个按钮干什么

```
标题栏  视频标题 · 当前进度 / 达标线                    —   ◎   ⟲
        （— 收成一条标题栏；◎ 收成页面上的悬浮球；⟲ 复位位置与大小）

[🛡 安全模式] [⚡ 暴力模式]      两个模式各占一页，切换会停掉另一个

[扫描]        按当前页面重新扫一遍视频列表
[刷新列表]    强制重扫：清缓存 + 逐个展开折叠章节 + 读服务端接口兜底
[只看未完成]  过滤掉已达标的视频
[▾ 列表]      收起/展开列表本体（列表太长时给下面的内容让位）
[⚙ 设置]      打开设置窗口（偏好设置都在这里）
[⏹ 急停]      停止队列与发包 + 暂停视频，并且**不会被自动恢复**

[视频列表]    序号 · 标题 · 进度条 · 状态；当前在看是**黄色**，已完成是绿色
              点其中一行 = 跳到那一节

[⏮] [▶ 播放 / 重播] [⏭]        上一节 / 播放（暴力模式下变成"播放并开始暴力"）/ 下一节

──── 当前模式页 ────
安全模式页：定时暂停、自动通过、自动重播、刷新列表、自动连播本页、只播这一节、事件体检、停止
暴力模式页：风险确认、发包参数、批量、点播放时按列表连播、诊断读数

──── 日志（主窗口底部）────
可滚动查看历史；默认自动跟到最新；你往上翻时不会被打断；点行数回到底部
```

**三处尺寸都能自己调，而且会记住**：

| 想调什么 | 怎么调 |
| --- | --- |
| 视频列表高度 | 拖**列表和下面那一页之间**的分隔条（鼠标移上去是上下箭头；往下拖列表变高） |
| 日志区高度 | 拖**日志区上面**那条分隔条（往上拖日志变高） |
| 整个面板大小 | 拖面板**右下角**的小手柄 |
| 面板位置 | 直接拖标题栏 |

拖完松手即记住（存在 Tampermonkey 存储里），刷新页面后照旧。
控制台等价入口：`__liruyun.proUi().listH()` / `__liruyun.proUi().logH()`（读；传数字即设）。

---

## 2. 设置窗口里的每一项

点 `⚙ 设置` 打开（位置会记住，可拖动；关闭只是把它藏起来，主面板不会变样）。

| 设置 | 默认 | 说明 |
| --- | --- | --- |
| **面板透明度** | 100% | 调低可以看穿面板；鼠标移到面板上会自动变清晰 |
| **自动静音** | 开 | 只要在播放就静音（页面自己把声音开回来也会被再次静音）。想听声音就关掉 |
| **弹窗拦截** | 开 | 站点弹的「禁止同时观看多个视频…」这类**浏览器原生弹窗**不再挡路，并顺带恢复被站点暂停的播放。未知的询问框照常弹给你自己选 |
| **完成后自动播下一节** | 开 | 达标后自动切到列表下一节。关掉：达标也**停在本节**，不会偷偷跳走 |
| **自定义达标进度** | 关 | 关着＝用课程页面自己写的要求（一般是 90%）；开着才用你填的百分比（1–100） |

> 「自定义达标进度」的建议：**别设得太低**。站点是按真实观看时长算进度的，
> 设得太低只会让你自己以为过了、课程页仍然显示未完成。

---

## 3. 视频列表：怎么保证"全"

脚本按这个顺序拼出列表：

1. **多容器择优**：扫描页面上所有可能装着活动链接的容器，按"能扫到多少条"打分取最好；
2. **展开折叠章节**：课程索引里折叠的章节条目**不在 DOM 里**，脚本会逐个点开再扫；
3. **服务端兜底**：用课程页自己的会话调用 Moodle 官方接口 `core_course_get_contents`（只读、无副作用），把整门课的活动补回来；
4. **按课程顺序合并**，并在列表上方写明 `共 N 节 · 已完成 M · 来源 …`。

列表还是少？按顺序做：

1. 点 `刷新列表`；
2. 打开控制台执行 `__liruyun.listInfo()`，里面会写：条数 / 来源容器 / 候选容器各扫到多少 / 展开了几个折叠容器 / 服务端兜底是否成功；
3. 把这一整段发出来（作者需要它才能判断是"容器找错了""折叠没展开""接口没读回来"还是"页面本来就没有"）。

---

## 4. 安全模式：自动连播

- `自动连播本页`：收集本节视频，按列表顺序一集一集播；每节达标就自动切下一节。
- `只播这一节`：只播当前这一节，播完不跳。
- **定时暂停**：每隔 N 分钟暂停 M 秒后自动继续（模拟正常观看节奏；间隔填 0 即关闭）。
- **自动通过**：学习确认弹窗出现时自动点掉（多事件类型自适应），然后恢复播放。
- **自动重播**：视频播完但课程还没判定完成时，自动重播一次。
- **停止**：停止队列（监控仍在运行）。
- **事件体检**：把确认弹窗上"哪种事件类型真的能解除遮罩"记下来（排查用）。

播放相关的看护是**一直开着**的（不依赖队列）：

- **停摆看护**：视频被暂停且没播完 → 尝试恢复播放；
- **冻帧看护**：`paused=false` 但时间不推进（缓冲 / 卡死）→ 重新 play，必要时轻微 seek 唤醒；
- 两者都不会在你按下 `⏹ 急停` 之后动手。

---

## 5. 暴力模式：它做什么、有什么风险、怎么用

**它做什么**：直接向学校的 `mod_fsresource_set_time` 接口连续上报观看时长，让进度快速到达完成要求。
它**不会**解除课程本身的其它要求（测验、作业），也**不会**给你假成绩。

**默认状态**：关闭；页签切过去也不会自动开始发包。

**三道闸门**：

1. 必须手动点 `⚠ 开始暴力`（或暴力模式页的 `▶ 播放并开始暴力`）；
2. 第一次启动会弹**确认框**，写清"次数上限 / 折算时长 / 会向服务器发异常请求"；
   - 确认一次之后，**当次会话**（这一个标签页）内不再重复询问；关掉标签页即失效；
   - 想恢复"每次都问"：暴力模式页里点一下 `风险确认：本次会话内不再问` 那一行；
3. 运行中有刹车：**达标即停**、**次数上限**、**连续多次失败即停**、**进度停滞即停**。

**常用参数**（暴力模式页）：

| 参数 | 默认 | 说明 |
| --- | --- | --- |
| 自动适配发包次数 | 开 | 按"视频总时长 × 目标百分比 ÷ 每次上报秒数"算需要发多少，不用手填 |
| 适配目标 | 100% | 按视频时长的百分比来推算 |
| 队列预算 | 5000 | 单次最多发多少（安全上限） |
| 停滞判定 | 150 | 连续多少发进度不涨就停 |
| 间隔 | 10ms | 每发之间的间隔（太小容易被风控，别乱调） |
| 每次上报时长 | 15 秒 | 每发"我看过 15 秒" |
| 达标即停 | 开 | 强烈建议保持开 |
| 点播放时按列表连播 | 关 | 开着：暴力页的播放按钮会把整个视频列表挨个刷完（跨页自动继续） |

**进度到 100% 但页面还显示旧的？** 页面读数是滞后的，脚本在达标后会自动**暂停 / 播放一次**催它刷新
（日志里会写 `催页面读数…41.2% → 100%`）。控制台也可以手动催：`__liruyun.bruteApi().nudge()`。

**想一次刷完整门课**：先把"点播放时按列表连播"打开，然后在暴力模式页点 `▶ 播放并连播整个列表`；
它会一节一节处理，中途随时可以 `⏹ 急停`。

---

## 6. 日志

- 位置：主窗口底部（**不会**跟着设置窗口走）。
- 能力：可滚动查看历史（缓冲区 400 行）、默认跟随最新、往上翻时不被抢、点行数回到底部。
- 高度：默认自动，拖它上面那根分隔条可改，松手记住。
- 防刷屏：同一条内容 60 秒内只写前两条，之后折叠成一行汇总（避免"同一句话刷满屏"）。
- 出问题先看它：`列表合并：…`（列表怎么来的）、`达标自停：…`（为什么停）、`催页面读数：…`、
  `页面被原生弹窗卡住 N.Ns`（站点弹窗没拦住时）。

---

## 7. 出问题怎么自查

| 现象 | 先做什么 |
| --- | --- |
| 面板没出现 | 看控制台有没有 `[liruyun]` 开头的报错；确认脚本已启用、页面已刷新 |
| 某个控件"不见了" | 控制台 `__liruyun.proUi().where()`：会写每个控件现在挂在谁身上 |
| 面板被挤没 / 滚动条怪 | `__liruyun.proUi().layout()`：面板、列表、日志各占多大 |
| 列表不全 | `刷新列表` → `__liruyun.listInfo()` 整段 |
| 弹窗又出现了 | `__liruyun.dialogInfo()`：拦了几条、被卡住几次 |
| 刷完了没判完成 | `__liruyun.bruteApi().nudge()` |
| 某个设置跟我选的不一样 | `__liruyun.prefs()` |
| 想全停 | `⏹ 急停` |

---

## 8. 数据与隐私

- 所有配置与进度记录都存在**本机**（Tampermonkey 存储 + `sessionStorage`），不上传任何地方。
- 脚本**只**访问 `moodle.scnu.edu.cn`；服务端兜底走的是页面自己的会话（`sesskey`），只读接口。
- 主要存储键：`liruyun_play_prefs`（偏好）、`scnu_liruyun_pro_ui`（面板布局与位置，含 `listH` / `logH`）、
  `liruyun_brute_cfg`（暴力参数）、`liruyun_brute_ack_mode`（风险确认模式）。

---

## 9. 免责声明

- 本脚本与华南师范大学、砺儒云课堂官方**无关**，是个人自用工具。
- 请仅用于自己已选课程的正常学习辅助，**不要**用它代替实际学习，也不要影响他人。
- 暴力模式属于"绕过正常观看流程"的操作，可能违反课程或学校的规定，也可能被风控记录；
  **是否使用、后果如何，由使用者自己判断与承担**。
